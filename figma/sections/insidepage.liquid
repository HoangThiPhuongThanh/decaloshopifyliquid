{%- style -%}

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}
<section class="banner-inside-page section-{{ section.id }}-padding">
  <div class="ctnr">
    <h2 class = "h2 title text-align-center">
      {{ section.settings.heading }}
    </h2>
  </div>
</section>
{% schema %}  
{
  "name": "Title page",
  "class": "section",
  "settings":[
    {
      "type": "inline_richtext",
      "id": "heading",
      "default": "Title ",
      "label": "Heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "unit": "px",
      "label": "Padding-top desktop",
      "default": 82
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "unit": "px",
      "label": "Padding-bottom desktop",
      "default": 82
    }
  ]
}
{% endschema %}